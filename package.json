{
  "name": "namerr-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@namerr/backend & npm run dev --workspace=@namerr/frontend",
    "build": "npm run build --workspaces",
    "install:all": "npm install"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
